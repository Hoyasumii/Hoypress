<nav class="navbar navbar-expand-lg bg-body-tertiary shadow mb-3 w-100">
    <div class="container-fluid">
        <a href="/home" class="navbar-brand fw-bold">Hoypress</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                
                <% if (isAuthenticated) { %>
                    <li class="navbar-item">
                        <a class="nav-link" aria-current="page" href="/articles">Artigos</a>
                    </li>
                    <li class="navbar-item">
                        <a class="nav-link" aria-current="page" href="/categories">Categorias</a>
                    </li>
                <% } else { %>
                    <% categories.slice(0,5).forEach(category => { %>
                        <li class="nav-item">
                            <a href="/categories/read/<%= category.slug %>" class="nav-link"><%= category.title %></a>
                        </li>
                    <% }) %>
                <% } %>

                <li class="navbar-item">
                    <% if (isAuthenticated) { %>
                        <a href="/users/logout" class="nav-link">Logout</a>
                    <% } else { %>
                        <a href="/users/login" class="nav-link">Acessar Conta</a>
                    <% } %>
                </li>
            </ul>
        </div>
    </div>
</nav>