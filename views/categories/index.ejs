<%- include("../partials/header") %>

<div class="container">

    <div class="bg-body-tertiary p-5 rounded shadow mb-3">
        <h1 class="display-1">Categorias</h1>
        <hr>
        <a class="btn btn-dark" aria-current="page" href="/categories/new">Criar Categoria</a>
    </div>

    <% if(data.length === 0) { %>
        <div class="alert alert-secondary" role="alert">
            Nenhuma categoria cadastrada!
        </div>
    <% } else { %>

        <div class="grid-container">
            <% data.forEach((category) => { %>
                <div class="bg-body-tertiary rounded shadow p-3 d-flex  justify-content-between align-items-center grid-item">
                    <div>
                        <a href="/categories/<%= category.slug %>" class="text-body link-underline link-underline-secondary link-underline-opacity-0 link-underline-opacity-100-hover">
                            <strong><%= category.title %></strong>
                        </a>
                    </div>
                    
                    <div class="d-flex gap-1 gap-md-2">
                        <a href="/categories/edit/<%= category.id %>" class="btn btn-dark bi bi-pencil"></a>
                        <button class="btn btn-dark bi bi-trash delete-button" data-bs-toggle="modal" data-bs-target="#confirm-delete-modal" data-id="<%= category.id %>"></button>
                    </div>
                </div>
            <% }); %>
        </div>

        <!-- <table class="table table-bordered table-hover shadow">
            <thead class="bg-body-tertiary">
                <tr>
                    <% Object.keys(data[0]).forEach((item) => { %>
                        <th scope="col"><%= item %></th>
                    <% }); %>
                    <th scope="col">Ações</th>
                </tr>
            </thead>
            <tbody>
                <% data.forEach((item) => { %>
                    <tr>
                        <% Object.values(item).forEach((value) => { %>
                            <td><%= value %></td>
                        <% }); %>
                        <td class="d-flex gap-1">
                            <a href="/" class="bi bi-eye btn btn-outline-success"></a>
                            <a href="/categories/edit/<%= item.id %>" class="bi bi-pen btn btn-outline-warning"></a>
                            <button class="bi bi-trash btn btn-outline-danger delete-button" data-bs-toggle="modal" data-bs-target="#confirm-delete-modal" data-id="<%= item.id %>"></button>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table> -->
    <% } %>

    
</div>

<div class="modal fade" id="confirm-delete-modal" tabindex="-1" aria-labelledby="confirm-delete-modal" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="confirm-delete-modal">Deseja remover o item?</h1>
            </div>
            <div class="modal-body">
                <p>Deletando essa categoria, você também irá deletar todos os artigos que possuem essa categoria.</p>
                <strong>Esta ação não pode ser desfeita.</strong>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                <form action="/categories/delete" method="post">
                    <input type="hidden" id="delete-id" name="id" value="">
                    <button type="submit" class="btn btn-danger" id="confirm-delete-button">Deletar</button>
                </form>
        </div>
    </div>
</div>

<%- include("../partials/footer") %>